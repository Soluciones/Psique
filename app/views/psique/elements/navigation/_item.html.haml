%li.menu-item
  = link_to item[:url] do
    = item[:name]
    = fa_icon 'angle-double-down', class: 'poar' if item[:submenu]

  - if item[:submenu]
    %ul.menu.menu-sub= render partial: 'psique/elements/navigation/item', collection: item[:submenu]
